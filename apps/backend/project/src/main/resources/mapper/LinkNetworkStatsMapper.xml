<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.luowb.shortlink.project.dao.mapper.LinkNetworkStatsMapper">

    <!-- 记录浏览器数据 -->
    <insert id="recordStatus">
        INSERT INTO t_link_network_stats (gid, full_short_url, date, network, cnt, create_time, update_time, del_flag)
        VALUES (#{gid}, #{fullShortUrl}, #{date}, #{network}, #{cnt}, NOW(), NOW(), 0)
        ON DUPLICATE KEY UPDATE cnt         = cnt + VALUES(cnt),
                                update_time = NOW()
    </insert>
</mapper>